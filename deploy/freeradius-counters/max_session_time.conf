sqlcounter max_session_time {
    counter_name = Max-All-Session-Time
    check_name = Max-All-Session-Time
    reply_name = Session-Timeout
    sqlmod-inst = sql
    key = User-Name
    reset = never
    query = "SELECT IFNULL(SUM(acctsessiontime),0) FROM radacct WHERE username = '%{${key}}'"
}
